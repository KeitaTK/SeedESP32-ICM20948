[project]
name = "icm20948-gauge-monitor"
version = "0.1.0"
description = "ICM-20948 NED 9-axis realtime analog-gauge monitor (USB serial firmware)"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
    "pyserial>=3.5",
]

[tool.uv]
package = false