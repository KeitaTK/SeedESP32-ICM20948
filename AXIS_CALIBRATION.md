# ICM-20948 軸マッピング実験手順（NED 統一のための実測）

本手順は、ICM-20948（AK09916 内蔵）の生データを **NED 系（X=北 / Y=東 / Z=下）** で
出力するために、センサ生軸 → 基板軸 → NED の「置換＋符号」を実験で確定するものです。

## 前提・注意

- 現行ファーム（`seq,ax,ay,az,gx,gy,gz,mx,my,mz` を 100Hz で USB シリアル出力）を書き込み済みであること。
- 磁石・金属・PC 本体・電源類から **50cm 以上離れた木製机** で実施する（既存 instruction.md と同じ）。
- 単位: 加速度 [g] / ジャイロ [rad/s] / 地磁気 [µT]。
- スクリプトは `visualizer/` 配下（uv プロジェクト）から実行する。

## 実行方法

```powershell
cd visualizer
uv run axis_capture.py --port COM9        # COM番号は環境に合わせる
```

- `Enter` … 記録開始（指示の姿勢をとってから押す）
- `r` … 直前のステップをやり直し
- `q` … 中断してここまでの結果を保存

出力:
- `capture_axis_summary.csv` … 各ステップの 平均/最小/最大（ラベル付き）
- `capture_axis_raw.csv` … 全サンプル（ラベル付き・再解析用）

---

## Phase A：加速度（基準 = 重力 1g）… 6姿勢

静止時の加速度計は「比力（＝重力の反力・**上向き**）」を測る。よって
**真下に向けた生軸は −1g、真上に向けた生軸は +1g** になる。

| ステップ | 操作 | 観察すべき生データ | 意味 |
|---|---|---|---|
| `accel_level` | 水平（部品面が上） | 1つの生軸だけ ≈ **+1g**、他 ≈ 0 | その生軸 = 「上向き」= Z 候補 |
| `accel_x_down` | 印字Xを真下へ（縦に立てる） | 1つの生軸だけ ≈ **−1g** | その生軸 = 基板X |
| `accel_x_up` | 印字Xを真上へ | 同じ生軸が ≈ **+1g** | X の符号確認 |
| `accel_y_down` | 印字Yを真下へ | 1つの生軸だけ ≈ **−1g** | その生軸 = 基板Y |
| `accel_y_up` | 印字Yを真上へ | 同じ生軸が ≈ **+1g** | Y の符号確認 |
| `accel_flip` | 裏返す（部品面が下） | `accel_level` と同じ生軸が ≈ **−1g** | Z（上下反転）の確認 |

> 注意: 各ステップでは **9軸すべて**（特に加速度3軸）を記録する。
> 「1つの生軸だけ」の判定は mean 列で行う。

### → マッピング導出（加速度・ジャイロ共通）

```text
x_ned (North = 基板X) = +( accel_x_down で −1g だった生軸 )
y_ned (East  = 基板Y) = +( accel_y_down で −1g だった生軸 )
z_ned (Down  = 基板Z) = −( accel_level で +1g だった生軸 )
```

例（capture_rotate.txt から既に確定済みの情報）:
- 水平で `az ≈ +1.01g` → 「上向き生軸 = az」→ **`z_ned = −az`**。

---

## Phase B：ジャイロ（基準 = 回転方向と右ねじの法則）… 4回転

加速度で確定した「置換＋符号」を**そのままジャイロにも適用**し、符号を検証する
（加速度とジャイロは同一パッケージ内で軸が一致するため、別に決め直す必要はない）。

NED の Z 軸は「下向き」なので、右ねじの親指を下に向けたとき =
**上から見て時計回りが正回転** になる。

| ステップ | 操作（記録中に回す） | 期待（NED変換後） |
|---|---|---|
| `gyro_yaw_cw` | 水平のまま上から見て時計回りに1回転 | `gz_ned > 0` |
| `gyro_yaw_ccw` | 上から見て反時計回りに1回転 | `gz_ned < 0` |
| `gyro_roll_pos` | 基板X軸まわりに右ねじ正方向へ回す | `gx_ned > 0` |
| `gyro_pitch_pos` | 基板Y軸まわりに右ねじ正方向へ回す | `gy_ned > 0` |

スクリプトは記録後に「回転の主軸とピーク符号（正=右ねじ正方向 / 負=逆方向）」を表示する。
**主軸が期待どおりで、NED 変換後の符号が上表と一致**すれば合格。
一致しない場合は Phase A で導出した変換（特に該当軸の符号）を見直す。

---

## Phase C：地磁気（基準 = 磁北と方位角）… 4方位

水平を保ったまま 4 方位を記録し、下表を埋める。
日本の伏角は ≈ 49°（磁場は「北かつ下向き」）なので、
水平成分 ≈ 30µT・下向き成分 ≈ 35µT が目安。

| ラベル | 操作 | mx_raw | my_raw | mz_raw |
|---|---|---|---|---|
| `mag_north` | 印字Xを磁北へ | ？ | ？ | ？ |
| `mag_east` | 右に90°（東） | ？ | ？ | ？ |
| `mag_south` | さらに90°（南） | ？ | ？ | ？ |
| `mag_west` | さらに90°（西） | ？ | ？ | ？ |

### → 地磁気マッピングの導出

1. **4方位で値がほぼ一定**の生軸 → 鉛直（Z）軸。
   - 日本は下向き正なので mean ≈ **+35µT** ならそのまま、≈ **−35µT** なら反転。
2. **北で正の最大・南で負の最大**になる生軸 → 北（X）軸。
3. **東で正の最大・西で負の最大**になる生軸 → 東（Y）軸。
4. 各軸を「北で正」「東で正」「下で正」になるよう符号調整。

### → 最終確認

NED 変換後の水平成分に対して

```text
heading = atan2(my_ned, mx_ned)   # [deg]、負なら +360
```

が `north=0° / east=90° / south=180° / west=270°` になること。
（基板Xを磁北に向けた状態で 0° = ビジュアライザの基準に一致）

---

## 実装状況と事後確認（2026/09/02）

確定した変換を `src/main.cpp` に**実装済み**（`AG_NED_SIGN` / `MAG_NED_SIGN` 定数と
`sampleAndSend()` 内の適用部）。

**※ 2026/09/02 追記**: 下流の RTIMULib が「水平静止で accel z=+1 / z-up」を期待するため、
座標系を **NED(Z下) → z-up 右手系** に変更した。「Z だけ反転」では左手系になるため
**Y と Z を同時に反転**している。

- accel / gyro: `x=+ax`, `y=+ay`, `z=+az`（生チップ軸そのまま）
- mag: `x=+mx`, `y=+my`, `z=-mz`（Z のみ反転し上向き正に統一）

基板X（印字）を北に置くと **NWU 相当（X=北, Y=西, Z=上）** の右手系になる。
水平静止では `az=+1`。

### 実測結果（capture_axis_summary.csv）の検証サマリ（旧NED版・参考）

※ 以下は NED（Z下）実装時の検証結果。現在は下記の z-up 右手系に変更済みのため、
「水平 → `az≈-1`」「heading = atan2(my,mx)」は**この段階の話**であり現在の出力には当てはまらない。

- **加速度**: 6姿勢すべて、NED変換後の値が物理期待値（±1g）に数mg以内で一致。
- **ジャイロ**: 4回転すべて符号一致。加速度と同一変換で正しいことを確認。
- **地磁気**: 水平回転の方向・90°刻みが一致。`mz` は4方位で一定＝鉛直軸と確認し、
  AK09916 のZが加速度と反転していること（`z=+mz` で下向き正）を確認。
- ⚠️ heading に約 −29° の一様なずれ（軸マッピングは正しい）。絶対方位を使う前に
  磁気キャリブレーション（8の字回転）と磁北合わせの再確認が必要。

### ファーム再書き込み後の最終確認（z-up 右手系）

シリアルモニタ（115200）で以下を確認する:

1. **水平**（部品面が上）→ `az ≈ +1g`、`ax, ay ≈ 0`
2. 上から見て回転 → **右ねじ**で `+z(上)まわり正 = 反時計回り` のとき `gz > 0`
   （時計回りなら `gz < 0`）
3. 地磁気のZが加速度と同向き（上向き正）になっている → 水平で `mz` が負の大きな値にならないこと
4. 最終的な heading の符号は下流の RTIMULib 側で検証する
   （RTIMULib の軸設定メニューで yaw 方向を調整）

